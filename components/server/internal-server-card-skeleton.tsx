import { Skeleton } from '@/components/ui/skeleton';

export default function InternalServerCardSkeleton() {
  return (
    <Skeleton className="flex h-full min-h-40 w-full rounded-md bg-card" />
  );
}
