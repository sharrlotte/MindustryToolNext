import React from 'react';

import PageClient from '@/app/[locale]/(user)/comments/[id]/page.client';

export const experimental_ppr = true;

export default function Page() {
  return <PageClient />;
}
