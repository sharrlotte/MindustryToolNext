export type ServerManager = {
  id: string;
  name: string;
  address: string;
  userId: string;
};

export type ServerManagerDetail = {
  id: string;
  name: string;
  address: string;
  userId: string;
  accessToken: string;
  securityKey: string;
};
