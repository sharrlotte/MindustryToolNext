export type ServerManager = {
  id: string;
  name: string;
  address: string;
  userId: string;
  status: string;
};

export type ServerManagerDetail = {
  id: string;
  name: string;
  address: string;
  userId: string;
  status: string;
  accessToken: string;
  securityKey: string;
};
